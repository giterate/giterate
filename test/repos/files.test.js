const { createRepos } = require('../helpers');

describe('.files()', function () {
  let repos;

  beforeEach(() => {
    repos = createRepos();
  })

  it('.forEach(fn)', function (done) {
    done();
  });
});
